# Модуль ядра Linux с таймером и логированием

Модуль ядра Linux, который периодически записывает сообщения в файл, и программа для управления его параметрами.

## 📁 Структура проекта

```
├── kernel_module/          # Исходный код модуля ядра
│   ├── Makefile           # Система сборки модуля
│   └── log_module.c       # Основной код модуля ядра
├── user_program/          # Программа для настройки
│   ├── Makefile           # Система сборки программы
│   └── config_tool.c      # Утилита конфигурации
└── README.md              # Этот файл
```

## ✨ Возможности

- **Периодическое логирование**: Модуль записывает сообщения с временными метками в файл с настраиваемым интервалом
- **Динамическая конфигурация**: Программа для изменения параметров модуля во время работы
- **Безопасные файловые операции**: Корректная обработка ошибок и управление ресурсами в пространстве ядра
- **Совместимость**: Собран для ядра Linux версии 6.12

## 🚀 Быстрый старт

### Требования
- Linux ядро 6.12 (или совместимое)
- Компилятор GCC
- Заголовочные файлы ядра (`linux-headers`)
- Права суперпользователя (sudo)

### Установка заголовочных файлов ядра
```bash
# Для Ubuntu/Debian
sudo apt-get install linux-headers-$(uname -r) build-essential

# Для Fedora/RHEL
sudo dnf install kernel-devel gcc make
```

